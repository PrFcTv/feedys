/**
 * Le serveur MCP — il expose les retours à l’agent de code du développeur.
 *
 * ⛔ Ce paquet est MIT. Rien de Quackback (AGPL) ne peut y entrer malgré le sujet
 *    commun : on n’en reprend que la forme des outils et le nommage. Une API
 *    n’est pas du code. 04-Architecture/licences.md.
 *
 * ⛔ Il n’importe rien de `apps/serveur`, et le lint le vérifie.
 *
 * Les trois outils — lister_retours, lire_retour, marquer_retour — arrivent en
 * P-011 (01-Specs/synthese.md §Le rendu MCP).
 */
export const VERSION = '0.0.0'
